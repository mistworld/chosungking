# 올바른 배포 명령어

## ❌ 잘못된 명령어
```cmd
wrangler deploy --config wrangler-do.toml --force
```
→ `--force` 플래그가 Wrangler 4.49.0에서 지원되지 않음

## ✅ 올바른 명령어
```cmd
wrangler deploy --config wrangler-do.toml
```

## "Assertion failed" 에러

이 에러는 Wrangler의 내부 경고일 수 있습니다. 
배포 자체는 성공했을 수도 있으니 Dashboard에서 확인해보세요.


